import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Verify we can build the widget tree without immediate crash
    expect(true, isTrue);
  });
}
